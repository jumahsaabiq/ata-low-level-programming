/**
 * main - the main function
 * description: Prints motivational text using fputs with stdout
 * return: 0
*/
#include <stdio.h>

int main(void)
{
    fputs("Keep pushing forward!\n", stdout);
    fputs("Never give up.\n", stdout);

    return 0;
}
